# Test parity report

The untouched kickoff suite contains 42 `Test*` functions. All 42 have a
one-to-one named Rust translation and currently pass. `benchutil_test.go`
contains helpers only. Per-file machine-readable results and command transcripts
are generated by `make parity-report`.

| Original file | Original | Translated | Passing | Rate |
|---|---:|---:|---:|---:|
| `diff_test.go` | 25 | 25 | 25 | 100% |
| `index_test.go` | 1 | 1 | 1 | 100% |
| `match_test.go` | 3 | 3 | 3 | 100% |
| `patch_test.go` | 9 | 9 | 9 | 100% |
| `stringutil_test.go` | 4 | 4 | 4 | 100% |
| **Total** | **42** | **42** | **42** | **100%** |

This is a language translation, so the original Go files cannot directly link
to a Rust crate. They remain byte-for-byte preserved and independently runnable;
the Rust files retain every test function/table and adaptation is disclosed in
`TEST_ADAPTATIONS.md`. Additional Rust checks cover byte-invalid input,
configuration `Send + Sync`, a multithreaded soak, both discovered upstream
behaviors, native API ergonomics, and the runnable CLI.
